import styled from 'styled-components';

export const ButtonWrapper = styled.div`
  display: flex;
  gap: 16px;
  button {
    min-width: 120px;
    padding: 8px;
    border-radius: 8px;
    font-size: 20px;
    font-weight: bold;
    cursor: pointer;
  }
`;
